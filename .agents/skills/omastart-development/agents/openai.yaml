interface:
  display_name: "Develop omastart"
  short_description: "Build and test the native plugin and website"
  default_prompt: "Use $omastart-development to implement this change with the appropriate isolated checks."
policy:
  allow_implicit_invocation: true
